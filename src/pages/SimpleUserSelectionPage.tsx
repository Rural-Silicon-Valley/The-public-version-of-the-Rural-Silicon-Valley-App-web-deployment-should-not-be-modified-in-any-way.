import { Box, Typography } from '@mui/material';

function SimpleUserSelectionPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>
        用户选择页面
      </Typography>
      <Typography>
        这是一个简化的用户选择页面用于测试。
      </Typography>
    </Box>
  );
}

export default SimpleUserSelectionPage;
